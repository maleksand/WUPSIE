import React from "react"
import { StyleSheet } from "react-native"

let styles = {}


// These is just as an example for now
styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20
    },
})

styles = StyleSheet.create({
    container2: {
        flex: 1,
        padding: 20,
        width: "100%"
    },
})

export default styles